# 🚀 NestJS + React 全栈项目

> **AI 驱动的全自动化开发工作流**

这是一个基于 NestJS 和 React 构建的全栈应用示例项目，采用 **Claude（把关）+ Codex（开发）** 的全自动化工作流。

---

## 🤖 全自动化开发工作流

### 工作流程

```
📝 编辑 todo.md → 提交 Git → 🤖 Codex 自动开发 → ✅ 自动测试 → 🔍 Claude 审查 → ✨ 合并部署
   (写需求)       (触发)        (写代码)          (质量检查)      (把关决策)      (上线)
```

### 💡 核心理念

- **你只需写需求** - 在 `todo.md` 文件中描述你想要的功能
- **Codex 自动编码** - AI 自动实现代码、编写测试
- **Claude 自动把关** - AI 审查代码质量、安全性、性能
- **自动反馈循环** - 不通过自动修改，直到代码合格
- **一键部署** - 审查通过后自动合并部署

---

## ⚡ 快速开始

### 第 1 步：添加开发任务

编辑项目根目录的 `todo.md` 文件：

```markdown
### 任务 1: 实现用户登录功能

**优先级**: 🔴 高

**状态**:
- [x] 待开发
- [ ] 开发中
- [ ] 待审查
- [ ] 已完成

**需求描述**:
作为用户，我希望能够使用邮箱和密码登录系统。

**前端需求**:
- [ ] 创建 Login.tsx 页面
- [ ] 实现表单验证（邮箱格式、密码长度 >= 6）
- [ ] 调用登录 API
- [ ] 登录成功后保存 token 并跳转

**后端需求**:
- [ ] 实现 POST /api/auth/login
- [ ] 验证用户密码（使用 bcrypt）
- [ ] 生成 JWT token
- [ ] 返回用户信息

**API 接口设计**:
POST /api/auth/login
请求：{ "email": "user@example.com", "password": "123456" }
响应：{ "success": true, "data": { "token": "xxx", "user": {...} } }

**验收标准**:
- [ ] 能够正确登录
- [ ] 密码错误显示提示
- [ ] 测试覆盖率 > 80%
```

### 第 2 步：提交到 Git

```bash
git add todo.md
git commit -m "docs: 添加用户登录功能开发任务"
git push origin main
```

### 第 3 步：等待自动化完成

1. **GitHub Actions 检测** - 自动检测 `todo.md` 变化
2. **Codex 开始开发** - 创建分支、编写代码、提交 PR
3. **自动化检查** - Lint、测试、构建自动运行
4. **Claude 自动审查** - 代码质量、安全性、性能审查
5. **自动合并部署** - 审查通过后合并并部署

### 第 4 步：查看进度

- **GitHub Issues** - 查看 Codex 创建的任务 Issue
- **Pull Requests** - 查看 Codex 提交的 PR
- **Actions** - 查看自动化检查和审查的运行日志

---

## 📊 工作流详解

### 1. 任务触发

当你提交 `todo.md` 到 Git 时：
- GitHub Actions 自动检测文件变化
- 解析新增的待开发任务
- 创建 Codex 任务通知 Issue

### 2. Codex 自动开发

Codex 接收到任务后：
- 创建开发分支（`feature/task-N-description`）
- 根据需求实现前后端代码
- 编写完整的测试用例
- 提交 PR

### 3. 自动化质量检查

PR 创建后自动运行：
- ✅ **ESLint** - 代码格式检查
- ✅ **TypeScript** - 类型检查
- ✅ **Jest** - 单元测试（覆盖率 >= 80%）
- ✅ **Build** - 构建测试
- ✅ **Security** - 安全扫描

### 4. Claude 自动审查

质量检查通过后，Claude 自动审查：
- 📋 **代码质量** - 是否符合项目规范
- 🔒 **安全性** - SQL 注入、XSS、CSRF 等漏洞
- ⚡ **性能** - 性能瓶颈、优化建议
- 🧪 **测试** - 测试覆盖率和质量
- 🏗️ **架构** - 架构设计合理性

**审查决策：**
- ✅ **APPROVE** - 通过，可以合并
- 💬 **COMMENT** - 有建议，需要改进
- ❌ **REQUEST_CHANGES** - 不通过，必须修改

### 5. 自动反馈循环

如果审查不通过：
1. Claude 在 PR 中评论具体问题
2. Codex 检测反馈并修改代码
3. 重新提交 PR
4. 重新运行检查和审查
5. 循环直到通过或转人工处理

### 6. 合并与部署

审查通过后：
- 人工最终确认（推荐）或自动合并
- 触发部署流程
- 更新 `todo.md` 任务状态为"已完成"

---

## 📁 项目结构

```
yjp-agent/
├── todo.md                          # 📋 任务清单（核心）
├── README.md                        # 项目说明
├── CLAUDE.md                        # 项目规范
│
├── .github/
│   ├── workflows/
│   │   ├── todo-trigger-codex.yml  # 检测 todo.md 变化
│   │   ├── claude-auto-review.yml  # Claude 自动审查
│   │   └── pr-check.yml            # 质量检查
│   └── PULL_REQUEST_TEMPLATE.md    # PR 模板
│
├── .claude/
│   ├── commands/                   # Claude 自定义命令
│   │   ├── code-review.md
│   │   ├── deploy-check.md
│   │   └── ...
│   └── settings.json
│
├── docs/
│   └── AUTO-WORKFLOW.md            # 完整自动化流程文档
│
├── server/                         # NestJS 后端
│   ├── src/
│   │   ├── main.ts
│   │   ├── app.module.ts
│   │   └── ...
│   ├── test/
│   ├── package.json
│   └── tsconfig.json
│
└── client/                         # React 前端
    ├── src/
    │   ├── App.tsx
    │   ├── index.tsx
    │   └── ...
    ├── public/
    ├── package.json
    └── tsconfig.json
```

---

## 🔧 技术栈

### 后端
- **NestJS** - 渐进式 Node.js 框架
- **TypeScript** - 类型安全
- **Express** - HTTP 服务器
- **Jest** - 测试框架

### 前端
- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Axios** - HTTP 客户端
- **React Testing Library** - 测试工具

### AI 工具
- **Claude Code CLI** - 代码审查、架构设计
- **Codex** - 自动编码实现
- **GitHub Actions** - 自动化 CI/CD

---

## 📚 详细文档

### 核心文档
1. **[todo.md](./todo.md)** ⭐ **最重要** - 在此添加开发任务
2. **[AUTO-WORKFLOW.md](./docs/AUTO-WORKFLOW.md)** - 完整的自动化流程说明
3. **[MONITORING.md](./docs/MONITORING.md)** 🔍 **实时监控** - 如何查看 AI 工作进度
4. **[CLAUDE.md](./CLAUDE.md)** - 项目编码规范

### GitHub 配置
- `.github/workflows/todo-trigger-codex.yml` - Codex 触发器配置
- `.github/workflows/claude-auto-review.yml` - Claude 审查配置
- `.github/workflows/pr-check.yml` - 质量检查配置

---

## 💡 最佳实践

### ✅ DO（推荐做法）

1. **详细描述需求**
   - 包含前后端需求
   - 提供 API 接口设计
   - 明确验收标准

2. **一次一个任务**
   - 避免同时添加多个复杂任务
   - 让 AI 专注完成一个功能

3. **及时更新状态**
   - 任务状态：待开发 → 开发中 → 待审查 → 已完成

4. **监控流程**
   - 查看 GitHub Actions 日志
   - 查看 Codex Issue 和 PR

### ❌ DON'T（避免做法）

1. **模糊的需求描述**
   - ❌ "做个登录功能"
   - ✅ "实现邮箱密码登录，包含表单验证、JWT 认证..."

2. **跳过验收标准**
   - AI 需要明确的验收标准来判断任务完成

3. **忽略审查反馈**
   - Claude 的反馈很重要，帮助提升代码质量

---

## 🚦 项目命令

### 安装依赖
```bash
# 安装所有依赖（前端 + 后端）
npm run install:all

# 或分别安装
cd server && npm install
cd client && npm install
```

### 开发模式
```bash
# 启动后端（终端 1）
npm run dev:server
# 或
cd server && npm run start:dev

# 启动前端（终端 2）
npm run dev:client
# 或
cd client && npm start
```

### 运行测试
```bash
# 后端测试
cd server && npm test

# 前端测试
cd client && npm test

# 测试覆盖率
cd server && npm run test:cov
cd client && npm run test:coverage
```

### 代码检查
```bash
# Lint 检查
cd server && npm run lint
cd client && npm run lint

# 自动修复
cd server && npm run lint:fix
cd client && npm run lint:fix
```

### 构建
```bash
# 构建后端
npm run build:server

# 构建前端
npm run build:client
```

---

## 🔍 故障排查

### Codex 没有开始开发

**检查：**
1. `todo.md` 是否正确提交到 main/develop 分支
2. GitHub Actions 是否成功运行
3. Codex Issue 是否创建

**解决：**
```bash
# 查看 GitHub Actions
仓库 → Actions 标签 → 查看 "Todo Changed" workflow

# 查看 Issues
仓库 → Issues → Filter by label: codex-task
```

### Claude 审查一直是 COMMENT

这是正常的，默认需要人工最终确认。

如需手动审查：
```bash
# 在本地使用 Claude Code CLI
claude

# 在 Claude 中执行
/code-review
```

### PR 检查失败

```bash
# 查看失败原因
仓库 → Actions → 点击失败的 workflow → 查看日志

# 常见问题修复
npm run lint:fix  # 修复 lint 错误
npm test          # 修复测试失败
npx tsc --noEmit  # 修复类型错误
```

---

## 🎯 示例：完整开发流程

### 需求：实现用户注册功能

#### 1. 在 todo.md 中添加任务

```markdown
### 任务 2: 实现用户注册功能

**优先级**: 🔴 高

**状态**:
- [x] 待开发

**需求描述**:
作为新用户，我希望能够注册账号。

**前端需求**:
- [ ] 创建 Register.tsx 页面
- [ ] 表单：用户名、邮箱、密码、确认密码
- [ ] 验证：用户名 2-20 字符，邮箱格式，密码 >= 8 位

**后端需求**:
- [ ] POST /api/users/register
- [ ] 检查邮箱是否已存在
- [ ] 密码加密（bcrypt）
- [ ] 创建用户记录

**API 接口设计**:
POST /api/users/register
请求：{ "username": "...", "email": "...", "password": "..." }
响应：{ "success": true, "data": { "user": {...}, "token": "..." } }

**数据库设计**:
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  username VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

**验收标准**:
- [ ] 能够成功注册
- [ ] 重复邮箱提示错误
- [ ] 密码正确加密
- [ ] 测试覆盖率 > 80%
```

#### 2. 提交
```bash
git add todo.md
git commit -m "docs: 添加用户注册功能开发任务"
git push origin main
```

#### 3. 自动化流程

- ✅ GitHub Actions 检测变化 (~1 分钟)
- ✅ 创建 Codex Issue (~1 分钟)
- ✅ Codex 开发代码 (~10-30 分钟)
- ✅ 提交 PR (~1 分钟)
- ✅ 自动化检查 (~5 分钟)
- ✅ Claude 审查 (~2 分钟)
- ✅ 人工确认合并 (由你决定)

#### 4. 查看结果

```bash
# 查看 PR
https://github.com/yjp1510586926/yjp-agent/pulls

# 查看 Claude 审查结果
在 PR 评论中查看

# 如果通过，点击 "Merge pull request"
```

---

## 🎉 开始使用

1. **Fork 或 Clone 本仓库**
2. **编辑 `todo.md`** - 添加你的第一个开发任务
3. **提交并推送** - `git add . && git commit -m "..." && git push`
4. **观察自动化** - 访问 GitHub Actions、Issues、Pull Requests
5. **享受 AI 开发** - Codex 干活，Claude 把关，你只管写需求！

---

## 📖 相关资源

- [NestJS 官方文档](https://docs.nestjs.com/)
- [React 官方文档](https://react.dev/)
- [Claude Code 文档](https://docs.anthropic.com/claude-code)
- [GitHub Actions 文档](https://docs.github.com/actions)

---

## 📄 许可证

ISC

---

**🚀 开始你的 AI 驱动开发之旅吧！**
